<h2> <%= @doctor.name %> </h2>
<p> Department: <%= @doctor.department %> </p>
<% @doctor.appointments.each do |app| %>
    <p>Patient: <%= link_to app.patient.name, patient_path(app.patient) %> </p>
    <p> Date and Time: <%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> </p>
    <hr>
<% end %>
