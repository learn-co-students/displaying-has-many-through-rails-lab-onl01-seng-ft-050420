<ul>
    <%@doctor.appointments.each do |appointment|%>
        <li> <%=link_to "#{appointment.patient.name} #{appointment.appointment_datetime.strftime("%B %d, %Y at %R")}", patient_path(appointment.patient)%> </li>
    <% end %>
</ul>