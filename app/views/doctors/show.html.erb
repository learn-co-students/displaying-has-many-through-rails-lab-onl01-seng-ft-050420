<h1>Name: <%= @doctor.name %></h1>
<h3>Department: <%= @doctor.department %></h3>

<h4>Appointments:</h4>
<ul>
    <%= @doctor.appointments.each do |appointment| %>
        <li><%= appointment.datetime %> - <%= link_to appointment.patient.name, patient_path(appointment.patient) %></li>
    <% end %>
</ul>