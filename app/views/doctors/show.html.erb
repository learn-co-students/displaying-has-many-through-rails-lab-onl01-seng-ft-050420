<%= @doctor.name %>
<%= @doctor.department %>

<% @doctor.appointments.each do |appn| %>
    <%= appn.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
    <%= link_to appn.patient.name, patient_path(appn.patient) %>
<% end %>