Appointments:
<ul>
    <% @patient.appointments.each do |appt| %>
        <li><%= appt.display_date_and_time %>
        <br>
        <li><%= link_to appt.doctor.name, doctor_path(appt.doctor.id) %>
    <% end %>
</ul>