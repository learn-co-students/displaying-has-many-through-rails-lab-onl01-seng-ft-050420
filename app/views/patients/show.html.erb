<ul>
<%@patient.appointments.each do |appointment|%>
    <li><%= link_to appointment.doctor.name, doctor_path(appointment.doctor)%><%=appointment.appointment_datetime.strftime("%B %d, %Y at %R")%></li>
<% end %>
</ul>