<h1>Patient Page</h1>
<h2><%= @patient.name %></h2>
Appointments:
  <% @patient.appointments.each do |appointment| %><br>
    <%= appointment.readable_datetime %>
    <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %>
  <% end %>
