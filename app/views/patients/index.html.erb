<h1>Patient Index</h1>
  <ul>
  <% @patients.each do |patient| %><br>
    <li>Name: <%= link_to patient.name, patient_path(patient) %></li>
    <li>Number of Appointments: <%= patient.appointments_count %></li>
  <% end %>
  </ul>
